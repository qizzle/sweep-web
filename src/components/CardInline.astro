---
const { heading } = Astro.props;
---

<div
  class="flex flex-col md:flex-row justify-between gap-4 md:gap-6 border-2 border-dotted p-4 md:p-6"
>
  <h1 class="w-full font-bit text-2xl md:text-3xl">{heading}</h1>
  <div class="w-full text-base md:text-lg">
    <slot />
  </div>
</div>
